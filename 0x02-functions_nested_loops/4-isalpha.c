#include "main.h"
/*
 * Description: isalpha  fuction - fuction to check if
 *
 * Return: return 1 is c if true else 0
 */
int _isalpha(int c)
{
	if (c >=97 && c<=122)
	if (c >=65 && c<=90)
		return (1);
	return (0);
}
